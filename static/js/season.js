// season.js — placeholder page module (Task 4+)
const season = {
    render() {
        const el = document.getElementById('page-season');
        if (!el) return;
        el.innerHTML = '<h2>season</h2><p>Coming soon...</p>';
    },
};
