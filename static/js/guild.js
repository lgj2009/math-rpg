"use strict";
// guild.js — placeholder page module (Task 4+)
const guild = {
    render() {
        const el = document.getElementById('page-guild');
        if (!el) return;
        el.innerHTML = '<h2>guild</h2><p>Coming soon...</p>';
    },
};
