// settings.js — placeholder page module (Task 4+)
"use strict";

const settings = {
    render() {
        const el = document.getElementById('page-settings');
        if (!el) return;
        el.innerHTML = '<h2>settings</h2><p>Coming soon...</p>';
    },
};
