// dashboard.js — placeholder page module (Task 4+)
const dashboard = {
    render() {
        const el = document.getElementById('page-dashboard');
        if (!el) return;
        el.innerHTML = '<h2>dashboard</h2><p>Coming soon...</p>';
    },
};
