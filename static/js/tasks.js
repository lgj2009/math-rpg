// tasks.js — placeholder page module (Task 4+)
const tasks = {
    render() {
        const el = document.getElementById('page-tasks');
        if (!el) return;
        el.innerHTML = '<h2>tasks</h2><p>Coming soon...</p>';
    },
};
