// progress.js — placeholder page module (Task 4+)
const progress = {
    render() {
        const el = document.getElementById('page-progress');
        if (!el) return;
        el.innerHTML = '<h2>progress</h2><p>Coming soon...</p>';
    },
};
