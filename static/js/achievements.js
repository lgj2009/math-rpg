// achievements.js — placeholder page module (Task 4+)
const achievements = {
    render() {
        const el = document.getElementById('page-achievements');
        if (!el) return;
        el.innerHTML = '<h2>achievements</h2><p>Coming soon...</p>';
    },
};
