// mistakes.js — placeholder page module (Task 4+)
const mistakes = {
    render() {
        const el = document.getElementById('page-mistakes');
        if (!el) return;
        el.innerHTML = '<h2>mistakes</h2><p>Coming soon...</p>';
    },
};
